# DocVeil Encryption Configuration

# IMPORTANT: Copy this file to .env and set a strong passphrase
# Command: cp .env.example .env

# Encryption passphrase for AES-256 encryption
# SECURITY REQUIREMENTS:
# - Minimum 32 characters recommended
# - Use a mix of uppercase, lowercase, numbers, and special characters
# - Generate using: openssl rand -base64 32
# - NEVER commit the .env file to version control
# - In production, use a secrets management service (AWS Secrets Manager, HashiCorp Vault, etc.)

ENCRYPTION_PASSPHRASE=your-super-secret-passphrase-change-this-to-something-very-strong-and-random

# Example of generating a strong passphrase:
# On macOS/Linux: openssl rand -base64 32
# On Windows (PowerShell): [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))
